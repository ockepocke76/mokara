# Engine deps — pinned to the versions the engine was validated on in the
# old repo (pre-port-detangled), minus everything streamlit.
pandas==2.3.3
numpy==2.2.6
numpy-financial==1.0.0
matplotlib==3.10.6
plotly==6.3.1
kaleido==1.1.0
reportlab==4.4.4
statsmodels==0.14.5
google-genai==2.19.0
toml==0.10.2
python-dotenv==1.0.1
requests==2.34.2
pyparsing==3.2.5
PyYAML==6.0.2
RestrictedPython==8.1
tabulate==0.9.0
psycopg2-binary==2.9.12
# pyarrow was a hidden transitive dep of streamlit in the old repo; the
# engine's parquet serialization needs it explicitly here.
pyarrow==25.0.1
sqlparse==0.5.3
google-cloud-storage==3.13.1
psutil==7.1.3

# Monitoring
google-cloud-monitoring==2.31.0
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0

# API layer (new)
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5

# Agentic strategy generation (W5) — langgraph only, no langchain model layer
langgraph==1.2.11
langgraph-checkpoint-postgres==3.1.2
